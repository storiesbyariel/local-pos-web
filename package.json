{
  "name": "local-pos-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "deploy": "npm run build && ./scripts/deploy-gh-pages.sh"
  },
  "devDependencies": {
    "vite": "^5.4.12",
    "vitest": "^2.1.8",
    "jsdom": "^25.0.1"
  }
}
